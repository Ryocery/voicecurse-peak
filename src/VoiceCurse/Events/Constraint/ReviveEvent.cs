namespace VoiceCurse.Events.Constraint;

public class ReviveEvent {
    // Revives yourself as a skeleton if you are dead.
    // Resource: Can only be used once per campfire.
    // Note: Different from ReanimateEvent, which affects other players.
}